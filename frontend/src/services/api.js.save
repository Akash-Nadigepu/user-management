const API_BASE = "http://<external-ip>:<port>"; // Replace with your service's LoadBalancer IP

export const fetchData = async () => {
  const res = await fetch(`${API_BASE}/api/endpoint`);
  return res.json();
};
